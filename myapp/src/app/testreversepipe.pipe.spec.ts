import { TestreversepipePipe } from './testreversepipe.pipe';

describe('TestreversepipePipe', () => {
  it('create an instance', () => {
    const pipe = new TestreversepipePipe();
    expect(pipe).toBeTruthy();
  });
});
